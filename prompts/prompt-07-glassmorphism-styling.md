# UI Styling & Glassmorphism Prompt

**User:**
```
The app works, but it looks a bit too plain. I want a modern, dark-mode 'Glassmorphism' aesthetic. Think deep slate backgrounds with glowing cyan and indigo ambient lights behind the cards. The cards themselves should have a slight backdrop-blur, semi-transparent backgrounds, and subtle borders. Go all out on the CSS to make it feel like a premium tool.
```

**Context:**
This imaginary prompt shows the user collaborating on the visual design direction. The AI would implement heavy Tailwind CSS utility classes like `backdrop-blur-lg`, `bg-slate-900/50`, and absolute positioned radial gradients to achieve the requested look across the application.
