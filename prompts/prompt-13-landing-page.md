# Landing Page Hero Section Prompt

**User:**
```
I need a landing page for `/?` when a user isn't logged in and doesn't have a specific `voterId`. It should have a big, bold hero section explaining what VibePoll is. Include a text input where they can manually type in a 8-character Poll ID to join a session if they don't have the direct link. Add some fake stats on the side showing the 'System Capacity' to make it look highly technical.
```

**Context:**
This imaginary prompt shows the user defining the public-facing identity of the app. The AI would build the landing container in `App.tsx`, complete with the manual ID joining logic and the decorative tech-stats side card.
