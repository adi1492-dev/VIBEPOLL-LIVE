# Confetti Animation Prompt

**User:**
```
When the poll timer hits zero, the ending feels a little anti-climactic. Can you build a custom ConfettiEffect component that shoots particles across the screen when the poll transitions from 'active' to 'ended'? Make it a reusable component so I can trigger it on the Presenter view and the Voter's screen if they won the trivia mode.
```

**Context:**
This imaginary prompt illustrates adding gamified polish. The AI would construct a complex `ConfettiEffect.tsx` file utilizing HTML5 Canvas or pure CSS keyframes to simulate particle physics upon the status change event.
