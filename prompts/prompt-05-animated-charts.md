# Animated Charts Implementation Prompt

**User:**
```
I need the poll results to look alive. Right now the data just snaps into place when a vote comes in. Can you build an AnimatedCharts component that supports both 'bar' and 'donut' chart types? The bars should grow smoothly over 500ms when the percentage changes, and the donut chart should rotate and fill its arcs seamlessly. Make it look premium.
```

**Context:**
This imaginary prompt demonstrates how the user requested the smooth data visualization layer. The AI would have responded by creating `AnimatedCharts.tsx` utilizing Framer Motion or pure CSS transitions along with SVG path calculations to create dynamic, high-quality visualizations.
